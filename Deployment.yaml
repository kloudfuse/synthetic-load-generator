apiVersion: apps/v1
kind: Deployment
metadata:
  name: trace-load-generator-6-22-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trace-load-generator-6-22-prod
  template:
    metadata:
      labels:
        app: trace-load-generator-6-22-prod
    spec:
      volumes:
        - name: config
          configMap:
            name: 6-22-prod
      containers:
      - name: trace-load-generator-6-22-prod
        image: us.gcr.io/mvp-demo-301906/kfuse/syntheticloadgenerator:latest
        env:
          - name: HOST_IP
            valueFrom:
              fieldRef:
                fieldPath: status.hostIP
          - name: JAEGER_COLLECTOR_URL
            value: "http://$(HOST_IP):14268"
          - name: TOPOLOGY_FILE
            value: "/topologies/topology.json"
        volumeMounts:
          - name: config
            mountPath: "/topologies/topology.json"
            subPath: file-from-cfgmap
---
